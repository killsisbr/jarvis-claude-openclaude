# DEPRECATED: Use ../docker-compose.yml na raiz do workspace
# Este arquivo foi substituido pela orquestracao unificada.
#
# Para iniciar todos os servicos:
#   cd D:\jarvis-claude
#   docker compose up -d
#
# Para iniciar apenas o openclaude worker:
#   docker compose up -d openclaude-worker
